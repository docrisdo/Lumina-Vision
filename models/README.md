# Modelos

Coloca aqui los archivos del detector de objetos.

Archivo esperado por defecto:

- `efficientdet_lite0.tflite`
- `coco_labels.txt`

Si usas otro modelo, actualiza `LUMINA_DETECTION_MODEL_PATH` en `.env`.
