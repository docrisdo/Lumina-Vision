numpy>=1.26,<3
opencv-python>=4.10,<5
pillow>=10.4,<12
python-dotenv>=1.0,<2
loguru>=0.7,<1
pytesseract>=0.3.10,<0.4
requests>=2.32,<3

# En Raspberry Pi suele ser mejor instalar Picamera2 desde apt:
# sudo apt install python3-picamera2 python3-libcamera
#
# Para Debian 13 / Python 3.13, Google publica `ai-edge-litert` como sucesor
# moderno de TensorFlow Lite runtime.
#
# La app intenta usar los runtimes en este orden:
# 1. tflite_runtime
# 2. ai_edge_litert
# 3. tensorflow.lite
